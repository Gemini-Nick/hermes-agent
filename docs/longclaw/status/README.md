# Longclaw Runtime Status

This directory is the local sync target for runtime-generated status artifacts.

- `runtime-status-latest.json`
- `runtime-status-latest.md`

They are generated by the Longclaw local runtime and are intentionally not committed.
Use them as local review surfaces that keep `hermes-agent` aligned with the current runtime queue and local control-plane state.
